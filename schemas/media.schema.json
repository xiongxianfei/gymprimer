{
  "canonical_svg": {
    "maximum_steps": 6,
    "minimum_steps": 3,
    "required_per_locale_text_field": "canonical_svg_text",
    "source_root": "media/svg"
  },
  "description": "M2 media metadata contract summary for canonical SVG references.",
  "kind": "media",
  "schema_version": "content-schema-v1",
  "status": "m2-contract",
  "supplemental_media_source_of_truth": false
}
